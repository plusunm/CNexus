# Layer 2 — FULL BOOT Verification Report

> Auto-generated by `scripts/layer2_full_boot_verify.py`.  
> Manual section **B10** requires Tauri desktop snapshot sign-off.

## Run Metadata

| Field | Value |
|-------|-------|
| **Run ID** | `{run_id}` |
| **Timestamp (UTC)** | `{timestamp_utc}` |
| **Sandbox** | `{sandbox_dir}` |
| **API Base** | `{api_base}` |
| **Overall** | **{overall_status}** |

---

## Gate Checklist (B1–B9)

| ID | Gate | Status | Detail |
|----|------|--------|--------|
| B1 | API Ready — `GET /v1/system/ready` → `operational_ready` | {b1_status} | {b1_detail} |
| B2 | AST Compliance — `assert_observability_compliance()` | {b2_status} | {b2_detail} |
| B3 | Governance Probe — `GET /governance/state` → `overall_stability_score` | {b3_status} | {b3_detail} |
| B4 | Observe Probe — memory stats + cross-shard `total_lines` | {b4_status} | {b4_detail} |
| B5 | Interact Smoke — `POST /v1/interact` (mock LLM) | {b5_status} | {b5_detail} |
| B6 | Canonical Trace — `t-{16hex}` in Σ.T daily shard | {b6_status} | {b6_detail} |
| B7 | SelfModel Domain Isolation — partial `self_model_*.json` mtime | {b7_status} | {b7_detail} |
| B8 | Σ.T Persistence — `interaction_step` rows in today shard | {b8_status} | {b8_detail} |
| B9 | Automated Summary — B1–B8 + L4-1..3 all green | {b9_status} | {b9_detail} |

---

## Layer 4 — Conscious Flow (L4-1..L4-3)

| ID | Gate | Status | Detail |
|----|------|--------|--------|
| L4-1 | SimulationEngine — `simulation_step` in Σ.T | {l4_1_status} | {l4_1_detail} |
| L4-2 | TrajectoryEvaluator — prune dangerous branches | {l4_2_status} | {l4_2_detail} |
| L4-3 | ReasoningTrace — assumption_seed + Σ.I mtime lock | {l4_3_status} | {l4_3_detail} |

---

## B10 — Tauri Client Snapshot (Manual)

| Field | Value |
|-------|-------|
| **Reviewer** | __________________________ |
| **Date** | __________________________ |
| **Build / Branch** | __________________________ |
| **Desktop smoke PASS** | ☐ Yes ☐ No |
| **Notes** | |

---

## Evidence Snippets

### Governance stability

```json
{b3_snippet}
```

### Trace store (cross-shard)

```json
{b4_snippet}
```

### Interact response (truncated)

```json
{b5_snippet}
```

### SelfModel domain mtimes

```
{b7_snippet}
```

---

## Layer 2 Sign-off

| Layer | Item | Status |
|-------|------|--------|
| X2 | SelfModel domain storage | Assumed (prior PR) |
| X3 | Observe interface + AST guard | B2 |
| L2-1 | Canonical trace ID | B6 |
| L2-2 | Daily Σ.T sharding | B4, B8 |
| L2-3 | FULL BOOT dry run | This report |
| L4 | Conscious flow stack | L4-1, L4-2, L4-3 |

**Layer 2 Status:** {layer2_status}
